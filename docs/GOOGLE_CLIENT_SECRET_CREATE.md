# Google OAuth Client Secret 생성 가이드

## 🔑 Client Secret이 없는 경우 해결 방법

OAuth 2.0 클라이언트 ID에 비밀번호(Client Secret)가 없는 경우, 새로 생성해야 합니다.

## 방법 1: 기존 클라이언트 ID에 비밀번호 추가 (권장)

### 1단계: Google Cloud Console 접속

1. **Google Cloud Console 접속**
   - https://console.cloud.google.com
   - 프로젝트 선택

2. **API 및 서비스 → 사용자 인증 정보**
   - 왼쪽 메뉴에서 **"API 및 서비스"** 클릭
   - **"사용자 인증 정보"** 메뉴 클릭

### 2단계: OAuth 2.0 클라이언트 ID 편집

1. **클라이언트 ID 클릭**
   - "PocketRithm" 클라이언트 ID 클릭
   - 또는 오른쪽의 **연필 아이콘(편집)** 클릭

2. **비밀번호 생성**
   - **"키 다시 만들기"** 또는 **"비밀번호 만들기"** 버튼 클릭
   - 또는 **"키 추가"** 버튼 클릭
   - 팝업에서 **"만들기"** 클릭

3. **비밀번호 복사**
   - ⚠️ **중요**: 비밀번호는 이 화면에서만 표시됩니다!
   - 생성된 비밀번호를 즉시 복사하세요
   - 형식: `GOCSPX-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

4. **저장**
   - **"저장"** 또는 **"완료"** 클릭

### 3단계: Supabase에 비밀번호 입력

1. **Supabase 대시보드**
   - https://supabase.com/dashboard
   - 프로젝트 선택

2. **Authentication → Providers → Google**
   - **Client Secret (for OAuth)** 필드에 복사한 비밀번호 입력
   - **Save** 클릭

## 방법 2: 새 OAuth 2.0 클라이언트 ID 생성 (대안)

기존 클라이언트 ID에 비밀번호를 추가할 수 없는 경우:

### 1단계: 새 클라이언트 ID 생성

1. **Google Cloud Console → 사용자 인증 정보**
2. **"+ 사용자 인증 정보 만들기"** → **"OAuth 클라이언트 ID"** 선택
3. **애플리케이션 유형**: **"웹 애플리케이션"** 선택
4. **이름**: PocketRithm (또는 원하는 이름)
5. **승인된 리디렉션 URI** 추가:
   ```
   https://kxoydaogtvkmjpoztnid.supabase.co/auth/v1/callback
   ```
   (실제 Supabase 프로젝트 URL로 변경)
6. **만들기** 클릭

### 2단계: 클라이언트 ID와 비밀번호 복사

1. 생성된 **클라이언트 ID** 복사
2. 생성된 **클라이언트 보안 비밀번호** 복사
   - ⚠️ 이 화면에서만 표시됩니다!

### 3단계: Supabase에 입력

1. **Supabase → Authentication → Providers → Google**
2. **Client ID (for OAuth)**: 새로 생성한 클라이언트 ID 입력
3. **Client Secret (for OAuth)**: 새로 생성한 비밀번호 입력
4. **Save** 클릭

## ⚠️ 중요 사항

### 비밀번호 보안
- 비밀번호는 **한 번만 표시**됩니다
- 복사하지 않으면 다시 볼 수 없습니다
- 안전한 곳에 보관하세요 (하지만 Git에는 커밋하지 마세요!)

### 기존 클라이언트 ID 사용 시
- 기존 클라이언트 ID에 비밀번호를 추가하면 기존 설정은 유지됩니다
- 새 클라이언트 ID를 만들면 기존 것과는 별개입니다

## 📝 체크리스트

- [ ] Google Cloud Console에서 OAuth 2.0 클라이언트 ID 편집
- [ ] 비밀번호 생성 (또는 새 클라이언트 ID 생성)
- [ ] 생성된 비밀번호 복사
- [ ] Supabase → Authentication → Providers → Google
- [ ] Client Secret (for OAuth) 필드에 비밀번호 입력
- [ ] Save 클릭
- [ ] 구글 로그인 테스트

## 🔍 문제 해결

### "키 다시 만들기" 버튼이 보이지 않는 경우
- 클라이언트 ID를 클릭하여 상세 페이지로 이동
- 또는 오른쪽의 **연필 아이콘(편집)** 클릭
- 상세 페이지에서 비밀번호 생성 옵션 확인

### 비밀번호를 잃어버린 경우
- 새 비밀번호를 생성해야 합니다
- 기존 비밀번호는 복구할 수 없습니다
- "키 다시 만들기"를 사용하여 새 비밀번호 생성

## 💡 팁

- 비밀번호를 생성한 후 즉시 Supabase에 입력하는 것을 권장합니다
- 비밀번호는 안전하게 보관하되, Git에는 절대 커밋하지 마세요
- 개발 환경과 프로덕션 환경에서 다른 클라이언트 ID를 사용할 수도 있습니다


