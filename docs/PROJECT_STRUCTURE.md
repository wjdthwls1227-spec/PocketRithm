# 포켓리즘 프로젝트 구조

## 📁 폴더 구조

```
PocketRithm/
├── app/                          # Next.js App Router
│   ├── (auth)/                   # 인증 관련 페이지 그룹
│   │   ├── login/
│   │   ├── signup/
│   │   └── onboarding/
│   ├── (dashboard)/              # 대시보드 및 메인 기능 그룹
│   │   ├── dashboard/
│   │   ├── expenses/
│   │   ├── income/
│   │   ├── statistics/
│   │   ├── retrospectives/
│   │   └── challenges/
│   ├── api/                      # API 라우트
│   │   ├── auth/
│   │   ├── expenses/
│   │   ├── ai/
│   │   └── webhooks/
│   ├── layout.tsx                # 루트 레이아웃
│   ├── page.tsx                  # 홈 페이지
│   └── globals.css               # 전역 스타일
│
├── components/                   # 재사용 가능한 컴포넌트
│   ├── ui/                       # 기본 UI 컴포넌트
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   ├── Card.tsx
│   │   └── ...
│   ├── expense/                  # 지출 관련 컴포넌트
│   ├── retrospective/            # 회고 관련 컴포넌트
│   ├── dashboard/                # 대시보드 컴포넌트
│   └── charts/                   # 차트 컴포넌트
│
├── lib/                          # 유틸리티 및 설정
│   ├── supabase/
│   │   ├── client.ts             # 브라우저 클라이언트
│   │   └── server.ts             # 서버 클라이언트
│   ├── utils/
│   │   ├── format.ts             # 포맷팅 함수
│   │   ├── validation.ts         # 검증 함수
│   │   └── ...
│   ├── ai/                       # AI 관련 함수
│   │   ├── insights.ts
│   │   └── reflections.ts
│   └── constants/                # 상수 정의
│
├── types/                        # TypeScript 타입 정의
│   ├── database.ts               # 데이터베이스 타입
│   └── api.ts                    # API 타입
│
├── supabase/                     # Supabase 관련 파일
│   ├── schema.sql                # 데이터베이스 스키마
│   ├── migrations/               # 마이그레이션 파일
│   └── functions/                # Edge Functions (선택사항)
│
├── public/                       # 정적 파일
│   ├── images/
│   └── icons/
│
├── hooks/                        # Custom React Hooks
│   ├── useAuth.ts
│   ├── useExpenses.ts
│   └── ...
│
├── styles/                       # 추가 스타일 파일 (선택사항)
│
├── package.json
├── tsconfig.json
├── next.config.js
├── tailwind.config.ts
├── postcss.config.js
├── .eslintrc.json
├── .gitignore
├── README.md
├── DEVELOPMENT_PLAN.md           # 개발 단계별 계획
└── PROJECT_STRUCTURE.md          # 이 파일
```

## 🔧 주요 파일 설명

### App Router 구조
- `app/`: Next.js 14 App Router 사용
- `(auth)`, `(dashboard)`: Route Groups로 레이아웃 분리
- `api/`: API 라우트 (서버 액션 또는 REST API)

### 컴포넌트 구조
- `components/ui/`: 재사용 가능한 기본 UI 컴포넌트
- 도메인별 컴포넌트는 기능별로 폴더 분리

### 라이브러리
- `lib/supabase/`: Supabase 클라이언트 설정
- `lib/utils/`: 유틸리티 함수
- `lib/ai/`: AI 관련 로직

### 타입
- `types/database.ts`: 데이터베이스 스키마 타입
- `types/api.ts`: API 요청/응답 타입

## 📝 네이밍 컨벤션

### 파일명
- 컴포넌트: PascalCase (예: `ExpenseForm.tsx`)
- 유틸리티: camelCase (예: `formatCurrency.ts`)
- 상수: UPPER_SNAKE_CASE (예: `API_ENDPOINTS.ts`)

### 컴포넌트
- 함수형 컴포넌트 사용
- Props 타입 명시

### 변수/함수
- camelCase 사용
- 의미 있는 이름 사용

## 🚀 다음 단계

1. `npm install` 실행하여 의존성 설치
2. `.env.local` 파일 생성 및 Supabase 설정
3. `supabase/schema.sql` 실행하여 데이터베이스 생성
4. `DEVELOPMENT_PLAN.md` 참고하여 단계별 개발 진행


